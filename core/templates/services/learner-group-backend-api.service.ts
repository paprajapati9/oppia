
// interface LearnerGroupInfoBackendDict {
//     learnerGroupSummaries: LearnerGroupSummaryBackendDict[];
// }

// export class LearnerGroupService {
  
//     /**
//      * Returns a promise that resolves to a list of learner group summaries.
//      */
//     async getLearnerGroupInfoAsync(){
        
//     }
//   }

//   /**
//    * It will have function getUsersAllLearnerGroupsAsync() that makes a GET request at /userlearnergroupshandler/ endpoint and returns a dictionary of learnerGroupSummaries and learnerGroupInvitationSummaries. The function will not take any arguments.
//     It has a function getLearnerGroupInfoAsync() that makes a GET request at /learnergrouphandler/<groupId> endpoint and returns a dictionary of learnerGroupSummaries and learnerGroupInvitationSummaries. The function will not take any arguments.
// It will have a function createNewLearnerGroupAsync() which creates a new learner group, it takes in groupTitle, groupDescription, invitations, syllabus as arguments to create the learner group and returns the groupId of the learner group created.
// It will have a function updateLearnerGroupAsync() which updates the learner group’s info, it takes groupId, groupTitle, groupDescription, invitations, syllabus as arguments and returns success or failure message.
// It will have a function deleteLearnerGroupAsync() which takes groupId as an argument and deletes that learner group.
// It will have function 
//    * 
//    */